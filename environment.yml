name: comp30830
channels:
  - defaults
dependencies:
  - python
  - sqlalchemy
  - requests
  - simplejson
  - ipython
  - pymysql
  - cryptography
  - pip
  - flask
  - pandas
  - scikit-learn
  - joblib
variables:
  DB_USER:
  DB_PASSWORD:
  DB_HOST:
  DB_PORT:
  DB_NAME:
  JCDECAUX_API_KEY: 
  JCDECAUX_CONTRACT:
  OPENWEATHER_API_KEY:
  OPENWEATHER_LAT:
  OPENWEATHER_LON:
  JCDECAUX_SCRAPE_INTERVAL:
  OPENWEATHER_SCRAPE_INTERVAL:
  MAIN_LOOP_SLEEP:
